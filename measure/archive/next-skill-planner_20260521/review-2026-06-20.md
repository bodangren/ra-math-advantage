# Track Review: next-skill-planner_20260521
**Review date:** 2026-06-20  
**Review window:** changes since 2026-06-19 (UTC).  
**Note:** This track has been archived to `measure/archive/next-skill-planner_20260521/`. Its implementation commits fall in the daily-report window ending 2026-06-19 05:04 +08:00; no commits appear with `git log --since="2026-06-19"`.

## Summary of Changes

The track was completed and archived. Key commits (from the track's archived `plan.md`):

- **Phase 1 — Contract & Schema** (`e455f49b`, `6328e26d`):
  - Created `packages/knowledge-space-practice/src/planner/types.ts` with `priorityWeightsSchema` (strict Zod, non-negative finite), `PriorityScore` discriminated union, `PlannerInput`/`PlannerOutput`.

- **Phase 2 — Scoring Terms** (`41ba9480`, `7d250b60`, `ba25c8fa`, `5ef074c4`):
  - Implemented `unlock-value.ts` (DFS descendant count), `goal-proximity.ts` (reverse BFS), `weakness-fit.ts` (stub to 0 pending Track 6).

- **Phase 3 — Composite Planner and Integration** (`c932981d`, `6bb677a6`, `a1675f34`, `bee22186`, `03791925`):
  - Implemented `priority.ts` (weighted composite) and `recommended-next.ts` (top-N ranker, ready-before-unknown, tie-break).
  - Wired `projectStudentVisualization` to use the planner; later restricted input to `ready` + `unknown` states.
  - Added adversarial coverage (`b9bba86f`, 37 tests).

- **Phase 4 — Docs & Doctor** (`a02fbd55`, `1d461811`, `a86e9cf1`, `492b54b8`, `899836b1`):
  - Updated `kst-srs.v2/SPECIFICATION.md` §7 with implementation-accurate contract details.
  - Fixed stale §10/§6.4 references to §7.
  - Archived the track and updated `measure/tracks.md`.

## Spec Alignment

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR1: unlock value | **Met** | `unlock-value.ts` + 16 tests |
| FR2: goal proximity | **Met** | `goal-proximity.ts` + 14 tests |
| FR3: weakness fit | **Met (stubbed)** | `weakness-fit.ts` returns 0; integrates Track 6 when available |
| FR4: composite priority | **Met** | `priority.ts` + 21 tests |
| FR5: recommendedNext top-N | **Met** | `recommended-next.ts` + 19 tests; visualization wired and filtered to ready+unknown |
| AC1–AC5 | **Met** | 360/360 knowledge-space-practice tests pass; tsc and lint clean |

## Code Quality Observations

- **Domain-neutral, pure, deterministic.** All planner functions operate on `PlannerNodeView`/`PlannerEdgeView` shapes and avoid app/convex imports.
- **Precomputation respected.** `computeUnlockValues`, `computeGoalProximities`, and `computePriorities` bulk APIs avoid per-request recomputation.
- **Visualization filter fix is correct.** `03791925` restricted planner input to `ready`/`unknown` so mastered/blocked/review-due nodes are not ranked into recommendations.
- **Spec parity test is valuable.** `kst-srs-v2-planner-parity.test.ts` ensures the Markdown spec stays aligned with the implementation.
- **Adversarial coverage is comprehensive.** 37 tests cover NaN/Infinity `topN`, self-loops, disconnected goals, empty graphs, shuffled input stability, and regression cases.
- **Minor concern:** `projectStudentVisualization` recomputes `computeNodeState` per ranked id rather than reusing `stateByNodeId` (auditor observation, not a correctness bug).

## Risks or Blockers

1. **weaknessFit is a stub.** Real integration with the misconception loop (Track 6) is required before the full composite priority is realized.
2. **Track is archived.** No further work is expected here unless Track 6 integration or a spec drift issue surfaces.
3. **No new commits in the strict 24h window.** The archival and final fixes were completed earlier; verify the archive metadata is consistent with `measure/tracks.md`.

## Recommended Next Actions

1. Confirm the archived track metadata in `measure/archive/next-skill-planner_20260521/metadata.json` is consistent with the current registry schema.
2. When Track 6 (misconception loop) is ready, create a new track or reopen a focused follow-up to replace the `weaknessFit` stub.
3. Monitor `kst-srs-v2/SPECIFICATION.md` §7 for drift; the parity test should catch it.
4. Consider optimizing `projectStudentVisualization` to reuse `stateByNodeId` when ranking nodes (low-priority code-quality improvement).
