# Track Review: study-hub-games-adoption_20260605 — 2026-06-24

## Summary of Changes (last 24h)

The track completed Phases 1–3 and was archived:

- **Phase 1 — Routes & glossary wiring:** Verified pre-existing IM3 routes (`/student/study/matching`, `/student/study/speed-round`) are linked from the study hub and student-gated via `requireStudentSessionClaims`. Exported `MatchingGame` and `SpeedRoundGame` from `packages/study-hub-core/src/index.ts` so the package surface provides the game primitives.
- **Phase 2 — Result persistence & mastery:** Verified game sessions call `internal.study.recordStudySession`, which updates `term_mastery` consistently with the flashcard SRS path. Empty/locked states fall back to `allTerms` when no module is selected.
- **Phase 3 — Verification:** Lint, `tsc --noEmit`, and targeted tests pass (9 study-hub-core tests + 43 IM3 study/glossary tests).
- Added `packages/study-hub-core/src/__tests__/games-exports.test.ts` to enforce the public export contract.
- **Closeout** — Track archived.

## Spec Alignment

| Spec requirement | Status |
|---|---|
| Routes/pages for matching + speed-round | **Met** — pre-existing routes verified and linked. |
| Glossary wiring via study-hub-core (no local copies) | **Partial** — games now exported from the package, but IM3 still consumes a local `@/components/student/MatchingGame` component. Future migration should switch IM3 to the package export. |
| Session results persistence + term-mastery feed | **Met** — pre-existing `recordStudySession` wiring verified. |
| Entry/empty states | **Met** — fallback to `allTerms` when no module selected. |
| Auth gating | **Met** — both pages call `requireStudentSessionClaims('/auth/login')`. |

## Code Quality Observations

- **TDD on adoption glue:** The export contract test is a good Red/Green artifact, but the core game logic was implemented upstream and only adopted here.
- **Verification:** No new test regressions; targeted suites green.
- **Boundary discipline:** No unrelated files modified.
- **Caveat:** The spec's "no duplicated term data" requirement is structurally satisfied (glossary consumed via shared package), but the game component itself is still imported locally in IM3. This is called out explicitly in the plan as acceptable for this track.

## Risks or Blockers

- **None severe.** Track archived.
- **Medium-term drift risk:** If IM3 continues to import a local `MatchingGame` component while the package export exists, the two copies can diverge. A follow-up track should migrate IM3 to the package component.

## Recommended Next Actions

1. Create a small migration track to replace `@/components/student/MatchingGame` imports in IM3 with `@math-platform/study-hub-core` imports.
2. Add an E2E or integration test that plays a full matching/speed-round session through the IM3 study hub to lock in the adoption path.
