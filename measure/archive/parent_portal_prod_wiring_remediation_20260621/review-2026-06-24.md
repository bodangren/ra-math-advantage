# Track Review: parent_portal_prod_wiring_remediation_20260621 — 2026-06-24

## Summary of Changes (last 24h)

The track remediated the 2026-06-21 completion audit finding that `/parent` was a static stub, completed verification, and was archived:

- **Phase 1 — Red tests:** Added `apps/integrated-math-3/__tests__/app/parent/parent-portal-prod-wiring.test.tsx` with 6 route-rendering tests failing against the static stub.
- **Phase 2 — Production wiring:**
  - Rewrote `app/parent/page.tsx` as an async server component calling `requireParentServerSessionClaims('/parent')`.
  - Added `convex/parent/visualization.ts` with `projectParentVisualizationQuery` (internalQuery) and a mock-ctx-friendly `projectParentVisualizationHandler`.
  - Added `components/parent/ParentPortalClient.tsx` composing `StudentSwitcher`, `ParentDashboard`, and `ParentEmptyStates` from live route data.
  - Updated `convex/_generated/api.d.ts` manually to register `parent/visualization`.
- **Phase 3 — Verification & closeout:** Targeted route tests 7/7 passing; lint clean; track archived; back-link added to archived `parent-portal_20260605/index.md`.

## Spec Alignment

| Spec requirement | Status |
|---|---|
| FR-1: `/parent` calls parent session guard | **Met** — `requireParentServerSessionClaims` used; non-parent sessions redirect. |
| FR-2: Fetch parent/student links + projection | **Met** — route queries parent visualization and links data. |
| FR-3: Add parent-safe visualization query | **Met** — `projectParentVisualizationQuery` exported and registered. |
| FR-4: Render dashboard/switcher/empty states from live data | **Met** — `ParentPortalClient` composes components from live data; server fallback covers no-links. |
| FR-5: Exclude teacher-only fields, fail closed | **Met** — handler fail-closes on missing/inactive links and non-student targets. |
| FR-6: Route-rendering tests, non-test caller proof | **Met** — 7 targeted tests pass; `ParentDashboard` has a non-test production caller. |
| AC1–AC7 | **Met** — all acceptance criteria satisfied. |

## Code Quality Observations

- **Strong remediation discipline:** The Red tests directly assert runtime route behavior rather than source-regex acceptance.
- **Fail-closed design:** Visualization handler rejects non-parent sessions and invalid link states at the backend.
- **Build-graph hygiene:** Intentionally skipped `graph.db` refresh per `test-strategy.md` to avoid the SQLite mtime re-dirty issue.
- **Manual generated-file update:** `convex/_generated/api.d.ts` was updated manually because `npx convex dev` requires a live deployment. This is acceptable but should be regenerated by CI or a deployment step to avoid drift.
- **TypeScript:** 0 errors attributed to track edits; pre-existing baseline errors (~311) correctly scoped out.
- **Lint:** Clean on track-relevant files.

## Risks or Blockers

- **None severe.** Track archived.
- **Medium risk — generated API drift:** Manual edits to `convex/_generated/api.d.ts` can become stale if the Convex schema changes. Ensure the next deployment runs codegen and commits the result.
- **Low risk:** The export-contract test scans only `convex/parent/links.ts`, not all `convex/parent/*.ts`; the new `visualization.ts` is covered by route tests but not by the export-contract scan. Consider broadening the scan in a follow-up.

## Recommended Next Actions

1. In the next IM3 deployment, run `npx convex dev`/`npx convex codegen` and verify `api.d.ts` matches the manual change.
2. Broaden `export-contract.test.ts` to scan the entire `convex/parent/` directory.
3. Add a lightweight E2E smoke test that logs in as a parent and loads `/parent` in a real browser context.
