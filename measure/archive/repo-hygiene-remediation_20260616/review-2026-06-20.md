# Track Review: repo-hygiene-remediation_20260616
**Review date:** 2026-06-20  
**Review window:** changes since 2026-06-19 (UTC), plus daily-report context where noted.

## Summary of Changes

The track advanced through Phases 3–5 in the review window:

- **Phase 3 (BM2 pre-existing test reds)** — completed.
  - `f07e1253` added a role-aware Dashboard link to `packages/app-shell/src/components/UserMenu.tsx` and fixed the `GradebookDrillDown` flaky timeout by converting `SubmissionDetailModal` from dynamic to static import and adding `role="dialog"` to the loading state.
  - `133f0b88` added a dedicated Red contract test `user-menu-dashboard-link.test.tsx`.
- **Phase 4 (IM3 React 19 eslint violations)** — completed.
  - `b8c35cb0` resolved 18 original + 5 newly-surfaced violations across 16 files by replacing effect-based state initialization with `useMemo`, lazy `useState` initializers, `queueMicrotask` deferred updates, module-scope component extraction, and `React.createElement` for dynamic tags.
  - `62a7ba0c` fixed the 3 remaining `react-hooks/exhaustive-deps` warnings in `PhaseCompleteButton.tsx` and `MatchingGame.tsx`.
  - The disabled React 19 rules block was removed from `apps/integrated-math-3/eslint.config.mjs`.
- **Phase 5 (Verification / clean state)** — completed.
  - `2d2e9c30` resolved the long-blocked Task 5.4 by moving unrelated dirty work to a WIP branch (`track-7-pending-remediation`), dropping the stale stash, and adding `__pycache__/` to `.gitignore`.
  - `git status --short` and `git stash list` are now empty on `master`.

Most commits in the window are `plan.md` updates recording SHAs, bounded commands, gate-blocker rationale, and live verification results.

## Spec Alignment

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR-1: clean working tree | **Met** | `git status --short` empty after `2d2e9c30` |
| FR-2: resolve stashes | **Met** | `git stash list` empty; WIP branch preserves recoverable work |
| FR-3/F-4: spec-compliance Phase 3/7 | **Partial** | Typed JSDoc deferred to `spec-compliance-and-process-integrity_20260612`; not this track's scope |
| FR-5: BM2 test reds | **Met** | 9/9 `user-menu.test.tsx`, 4/4 `user-menu-dashboard-link.test.tsx`, 5/5 `GradebookDrillDown.integration.test.tsx`, 14/14 `SubmissionDetailModal*.test.tsx` pass |
| FR-6: React 19 lint violations | **Met** | 4 targeted rules run at error level → 0 errors; 111/111 IM3 regression tests pass |
| FR-7/F-8: tsc/lint pass | **Partial** | BM2 lint passes; IM3 lint fails on 2 pre-existing warnings in `student-flow.test.ts` (owned by `primitive-layer-contract_20260615`); tsc fails on 388 IM3 / 31 BM2 errors from the same track |
| AC-1/AC-2 clean state | **Met** | documented in plan |
| AC-3/AC-4 lint | **Partial** | BM2 clean; IM3 blocked by unrelated track |
| AC-5/AC-6 tsc | **Partial** | blocked by unrelated track |
| AC-7 React 19 rules | **Met** | rules re-enabled, 0 violations |
| AC-8 BM2 tests | **Met** | targeted gates pass |

## Code Quality Observations

- **React 19 fixes use idiomatic patterns.** `useMemo` replaces effect-based derivation, lazy `useState(() => Date.now())` replaces `Date.now()` in render, `queueMicrotask` defers state updates where necessary, and sub-components are extracted to module scope. These are the standard React 19 purity patterns.
- **Minimal, targeted changes.** `62a7ba0c` changes only 3 lines across 2 files to resolve exhaustive-deps warnings.
- **UserMenu Dashboard link is simple and composable.** It respects the existing optional `dashboardHref` prop and falls back to role-based routing only when the prop is absent.
- **GradebookDrillDown fix addresses the root cause.** Static import removes the microtask/timeout race that made the dialog appear after `waitFor` expired.
- **Plan documentation is thorough but verbose.** The plan now contains ~1,100 lines with extensive gate-blocker narrative. This is useful context but increases maintenance burden; a condensed closeout summary would help future readers.

## Risks or Blockers

1. **Cross-track tsc/lint failures remain.** The 388 IM3 and 31 BM2 tsc errors and the 2 IM3 lint warnings are explicitly attributed to `primitive-layer-contract_20260615` / `practice-variant-rename_20260521`. Until those tracks land, this track cannot fully satisfy AC-3–AC-6.
2. **WIP branch `track-7-pending-remediation` must not be lost.** It holds 16 files from other tracks. There is no documented expiration/merge plan for it.
3. **Full CI test suites time out.** `CI=true npm run test --workspace=apps/integrated-math-3` and `apps/bus-math-v2` each time out at 15 minutes (340/339 files). Targeted regression gates pass, but aggregate acceptance is unverified.
4. **Phase 3/7 of `spec-compliance-and-process-integrity_20260612` are deferred.** Those are explicitly out of scope for this track, but they are prerequisites for a fully green repo.

## Recommended Next Actions

1. Coordinate with `primitive-layer-contract_20260615` to complete its schema rename; then re-run `npx tsc --noEmit` for IM3 and BM2.
2. After tsc is clean, re-run `npm run lint --workspace=apps/integrated-math-3` and confirm 0 warnings.
3. Run the full CI test suites in an environment that avoids the 15-minute timeout (e.g., split by test directory or increase timeout).
4. Add a recovery/merge plan and owner for `track-7-pending-remediation` to `tech-debt.md` or the relevant track plans.
5. Trim the `plan.md` Phase 4/5 gate-blocker narrative into a concise closeout appendix once blockers are resolved.
