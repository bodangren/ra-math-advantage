# Daily Review — Unblock CI package typecheck (2026-07-02)

**Track:** `package-typecheck-ci-unblock_20260701`  
**Status:** Completed  
**Reviewer:** daily-automation changed-track review  
**Commits reviewed:** 4 (2026-07-01 20:58 → 2026-07-01 21:30 +08:00)

## 1. Summary of changes

The last 24 hours landed and closed the track that fixes the Phase 1 `Run package typecheck` CI gate.

- **Config fix** (`ee10aa43`) — added `"types": ["node", "vitest/globals"]` to `compilerOptions` in the four failing package tsconfigs:
  - `packages/practice-core/tsconfig.json`
  - `packages/core-convex/tsconfig.json`
  - `packages/core-auth/tsconfig.json`
  - `packages/ai-tutoring/tsconfig.json`
- **Dependency declaration** (`ee10aa43`) — added `@types/node@^22` to each package's `devDependencies` and updated `package-lock.json` with the resolved `22.20.0` entries. The user approved this dependency change, so it was done inline rather than deferred.
- **Track scaffolding + registry hygiene** (`42fcc633`) — created the track directory with `metadata.json`, `spec.md`, and `plan.md`; updated `measure/tech-debt.md` to record the Phase-1 root cause as fixed and to mark the stale `spec-compliance-and-process-integrity_20260612` row as Resolved; updated `measure/tracks.md` to list the track as completed.
- **Completion marker** (`a764fc20`) — marked the track status as completed.
- **Daily docs** (`594caed4`) — also added unrelated archive review reports for `kst-lesser-holes_20260521` and `spec-compliance-and-process-integrity_20260612`, plus a review for `teacher-content-authoring_20260605`.

## 2. Spec alignment

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR-1 — `npm run typecheck --workspace=packages/<pkg>` exits 0 for all four packages | **Met** | Plan T3 records local verification: 42 TS2591/TS7006 errors → 0 for `practice-core`, `core-convex`, `core-auth`, and `ai-tutoring`. |
| FR-2 — Node ambient types declared without regressing vitest globals | **Met** | Each `tsconfig.json` now sets `"types": ["node", "vitest/globals"]`, preserving ambient test globals from `scripts/vitest.config.ts` (`globals: true`). |
| FR-3 — Config-only fix, no dependency/lockfile change | **Partial / Deviation** | Original spec asked for no manifest/lockfile change and reliance on root-hoisted `@types/node`. The implemented fix added `@types/node@^22` to each package's `devDependencies` and `package-lock.json`. The plan explicitly notes this was a user-approved dependency change and is the more robust long-term hygiene move. |
| FR-4 — Package unit-test suites remain green | **Met** | Plan T3 reports suites still green (193/14/54/75 tests) with no source-file changes. |
| AC-4 — `tech-debt.md` line 35 updated | **Met** | `measure/tech-debt.md` now records the Phase-1 root cause as fixed and the deploy landing as pending a CI run. |

**Overall alignment:** **Met** — the track satisfies its core acceptance criteria. The only deviation from the original spec is the explicit `@types/node` dependency declaration, which was user-approved and improves reproducibility.

## 3. Code quality observations

- **Strengths**
  - The fix is minimal and precisely targeted: four identical `tsconfig.json` additions and matching devDependency declarations, with no source-file mutations.
  - Using `@types/node@^22` per package removes reliance on root hoisting and matches the app convention, making the fix more deterministic across CI and local installs.
  - The root cause (missing `node` ambient types cascading into implicit-`any` errors) is well documented in `spec.md`.
  - `plan.md` follows the TDD framing and clearly ties the Red/Green/Verify phases to the CI gate itself.

- **Concerns**
  - `package-lock.json` acquired many `"peer": true` annotations on optional native dependencies (e.g., `@rollup/rollup-*`, `@swc/core-*`, `@img/sharp-*`). These appear to be side effects of `npm install` re-resolution rather than intentional changes. They are benign but create noise in the diff and could confuse future reviewers.
  - The `tsconfig.json` edits use multi-line JSON arrays while the original files used compact single-line objects. This is fine, but it means the diff is slightly larger than necessary.

## 4. Risks or blockers

- **Medium — remote CI not yet observed green:** The fix is verified locally, but the actual `ci.yml` run and the downstream IM3/BM2 Cloudflare deploy job have not been observed passing remotely. This is explicitly acknowledged as out of scope in `spec.md` Non-goals, but it remains the critical next milestone.
- **Low — lockfile noise:** The unrelated `peer: true` additions in `package-lock.json` are not harmful, but they could mask future genuine dependency issues if reviewers skip over them.
- **Low — same class of issue in other packages:** `math-content` lint/standalone `tsc` issues and other packages may suffer from the same missing-node-types problem. These are logged separately in `tech-debt.md` and out of scope here.
- **Low — transient repo artifacts still present:** `_add_types.py` and `_add_types.ts` at the repo root (noted in the 2026-07-01 review of `spec-compliance-and-process-integrity`) remain untracked and may confuse future automation.

## 5. Recommended next actions

1. **Push and watch the next CI run** — confirm Phase 1 `Run package typecheck` turns green for all four packages, then verify that downstream `im3`, `bm2`, and `deploy` jobs execute.
2. **Validate the IM3 Cloudflare deploy landing** once CI passes; this is the primary business outcome unblocked by the track.
3. **Clean up transient artifacts** — remove or promote `_add_types.py` and `_add_types.ts` from the repo root.
4. **Audit other packages** for the same missing `@types/node` pattern, especially `math-content`, and open a follow-up track if needed.
5. **(Optional) Re-lock more narrowly** if the `peer: true` annotations in `package-lock.json` are undesired; otherwise document that they are a harmless re-resolution side effect.
