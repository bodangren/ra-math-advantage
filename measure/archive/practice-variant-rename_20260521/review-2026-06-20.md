# Track Review: practice-variant-rename_20260521
**Review date:** 2026-06-20  
**Review window:** changes since 2026-06-19 (UTC).

## Summary of Changes

The track completed all four phases in the review window:

- **Phase 1 — Contract & Schema** (`96fd073f`, `ad4eb066`, `4953e3c9`):
  - Renamed `problemFamilyId → variantKey`, `ProblemFamily → PracticeVariant`, etc., in `packages/practice-core`.
  - Updated `practice-item.ts`, `problem-family.ts`, `index.ts`, and the Convex schema (`srs_cards.problemFamilyId → variantKey`).
  - Added `apps/integrated-math-3/convex/migrations/rename-problem-family.ts`.
  - Added Red contract tests in `practice-item.test.ts` and `rename-problem-family.test.ts`.

- **Phase 2 — Engine Rename** (`ff285065`, `0ccd8ab4`, `9ca5281c`, `80cfa0ea`):
  - Renamed across `packages/srs-engine`: scheduler, contract, objective-proficiency, srs-proficiency, adapters, submission adapter.
  - Updated `minProblemFamilies → minVariants`, `ProblemFamilyEvidence → PracticeVariantEvidence`.
  - Implemented single-variant default (`variantKey = objectiveId`).

- **Phase 3 — Projection, App Rename, Migration** (`f5b91fbb`, `b45a8b24`, `59f37118`, `f4ca974e`):
  - Renamed in IM3 `lib/srs/convexCardStore.ts` and Convex call sites (`cards.ts`, `processReview.ts`, `submissionSrs.ts`).
  - Flipped P2 test assertions in `contract.test.ts`, `scheduler.test.ts`, `submission-srs-adapter.test.ts`.
  - Added governance test `__tests__/governance/practice-variant-app-callsite-rename.test.ts`.

- **Phase 4 — Docs & Doctor** (`5355abb1`, `5a2f2218`, `b27004ee`, `a83134ac`):
  - Added kst-srs.v2 §12.1 (Practice Variant Boundary) and §13.4 (Practice Variant Contract).
  - Removed remaining stale `problemFamily*` identifiers in `practice-core` (`index.ts`, `problem-family.ts`, `timing-baseline.ts`).
  - Renamed IM3 `convex/timing_baseline.ts` and its test to `variantKey`.
  - Completed Phase 4 UMV.

## Spec Alignment

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR1: rename throughout in-scope packages | **Met** | `no-stale-problem-family.test.ts` reports 0 legacy identifiers in `practice-core`, `srs-engine`, `knowledge-space-practice` |
| FR2: Card keyed by `variantKey`; single-variant default | **Met** | `scheduler.ts` and `objective-proficiency.ts` use `variantKey`; default implemented |
| FR3: no new node kind | **Met** | variants remain below graph resolution |
| FR4: Convex schema migration | **Met** | `rename-problem-family.ts` migration module and tests added; schema renamed |
| FR5: call sites and tests green | **Met** | practice-core 193/193, srs-engine 232/233, knowledge-space-practice 366/366, root CI 285/285 |
| AC1–AC5 | **Met/Partial** | AC1–AC4 met; AC5 boundary lints/tsc/tests met for in-scope packages |

## Code Quality Observations

- **Mechanical, behavior-preserving refactor.** The rename is consistent and the proficiency algorithm is unchanged, matching the non-functional requirement.
- **Migration is reversible/verifiable.** The migration module operates against a test in-memory Convex store and can be dry-run before production application.
- **Scope discipline is good.** Out-of-scope packages (`packages/math-content`, `apps/integrated-math-2`, `apps/bus-math-v2`) retain legacy identifiers, which is explicitly documented.
- **Spec updates are accurate.** §12.1 and §13.4 correctly describe the variant boundary and contract.
- **Red-phase cleanup was noisy.** Multiple attempts were required to satisfy the supervisor gate, including reverting 37 unrelated files. While the final commits are clean, the worktree churn indicates the gate's `non_test_source_changes_since` logic needs tuning (already recorded in `tech-debt.md`).

## Risks or Blockers

1. **One flaky test in srs-engine.** Phase 4 evidence notes `232/233 passed (1 pre-existing flaky timing test)`. This should be stabilized or marked with `test.retry`.
2. **Live Convex migration dry-run deferred.** The migration module is tested in-memory; a live `npx convex run migrations:renameProblemFamilyToVariantKey --dry-run` has not been recorded.
3. **Out-of-scope legacy identifiers remain.** While intentional, `math-content` and seed files still reference `problemFamilyId`; these must be addressed in follow-up tracks or the rename will remain incomplete at the monorepo level.
4. **Schema rename blocks other tracks.** The `primitive-layer-contract_20260615` track's tsc failures (388 IM3 errors) are attributed to this schema rename being incomplete at call sites outside this track's scope.

## Recommended Next Actions

1. Stabilize or annotate the flaky srs-engine timing test.
2. Run the live Convex migration dry-run on a staging/production-like dataset and record the result.
3. Create follow-up tracks for `packages/math-content` and IM2/IM3 seed files to finish the monorepo-wide rename.
4. Coordinate with `primitive-layer-contract_20260615` / `repo-hygiene-remediation_20260616` to resolve the tsc blast radius from the schema rename.
5. Archive this track once the live migration is verified and follow-ups are created.
