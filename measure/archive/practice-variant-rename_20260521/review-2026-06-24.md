# Track Review: practice-variant-rename_20260521 — 2026-06-24

## Summary of Changes (last 24h)

No source-code or spec changes were committed in the last 24h. The only activity was closeout bookkeeping:

- `docs(track-7): add commit SHAs to all sub-tasks in plan.md for closeout gate` — backfilled SHA evidence in `plan.md`.
- `measure(track-7): closeout practice-variant-rename — archive, metadata, manifest` — moved the track directory to `measure/archive/`, updated `measure/tracks.md`, and wrote the closeout manifest.

The substantive rename work (`problemFamilyId` → `variantKey` across `practice-core`, `srs-engine`, `knowledge-space-practice`, and IM3 app-layer call sites) was completed in earlier commits.

## Spec Alignment

| Spec requirement | Status |
|---|---|
| FR1: Rename identifiers across packages | **Met** — earlier Phase 1–3 commits delivered the rename. |
| FR2: `Card = student × objective × variantKey` | **Met** — implemented and tested. |
| FR3: No new node kind | **Met** — variants remain below graph resolution. |
| FR4: Convex schema migration | **Met** — migration script and tests added. |
| FR5: Call sites and tests green | **Met** — verification documented in plan. |
| AC1–AC5 | **Met** — earlier verification documented. |

## Code Quality Observations

- **Closeout hygiene:** SHA evidence backfilled; archive move clean.
- **No new code risk:** Only Measure artifacts changed in this window.

## Risks or Blockers

- **None severe.** Track archived cleanly.
- **Low-risk note:** The plan.md notes that full `npm run lint && tsc --noEmit && CI=true npm run test` was the Green closeout gate. Since no code changed in the last 24h, those gates remain satisfied by the earlier committed state.

## Recommended Next Actions

1. None beyond confirming the archived `plan.md` SHA evidence is complete and readable.
