# Track Review: unified-auth-monorepo_20260609 — 2026-06-24

## Summary of Changes (last 24h)

The track completed all four phases, passed final acceptance, and was archived:

- **Phase 1 — Audit & classification:** Inventoried BM2 auth exports, classified 12 exports as (a) identical-to-package: 1, (b) generalizable-into-package: 11, (c) BM2-specific: 0. Authored the decision doc under `decisions/auth-export-classification.md`.
- **Phase 2 — Promote shared logic:** Added parameterized request-guard functions to `packages/core-auth/src/request-guards.ts`, covering cookie-store injection, response/redirect discriminated unions, role-list options, and active-credential verifier callbacks. Added 54 core-auth parity tests.
- **Phase 3 — Thin app wrappers:** Reduced BM2 `lib/auth/server.ts` from 290 to 163 lines, making it a thin composition over `core-auth`. IM3 wrapper retained inline helpers because its test mock stubs only `verifySessionToken` from `core-auth`; this residual duplication is tracked in `measure/tech-debt.md`.
- **Phase 4 — Verify & reconcile:** BM2 + IM3 auth/middleware tests green (88 tests total), `doctor.sh` green, no boundary violations.
- **Closeout** — Track archived.

## Spec Alignment

| Spec requirement | Status |
|---|---|
| FR1: Diff & classify BM2 exports | **Met** — 12 exports classified in decision doc. |
| FR2: Promote common logic to core-auth | **Met** — `request-guards.ts` added with parameterized options. |
| FR3: Thin BM2 wrapper | **Met** — 290 → 163 lines; only app-specific composition remains. |
| FR4: Evaluate direct re-export | **Met** — evaluated and rejected due to per-app cookie names/redirects/verifiers. |
| FR5: No duplicated logic | **Met for BM2; partial for IM3** — residual duplication tracked as tech debt. |
| AC1–AC5 | **Met** — 88 auth tests pass, doctor green, behavior unchanged. |

## Code Quality Observations

- **Excellent TDD discipline:** Phase 1 Red contract test fails on missing decision doc; Phase 2 Red parity tests fail on missing exports; both flipped Green cleanly.
- **Boundary safety:** `packages/*` does not import Next.js APIs; app-specific Next.js dependencies stay in app wrappers.
- **Test coverage:** 54 new core-auth tests + 33 BM2 lib/auth + 45 IM3 lib/auth + 7 IM3 middleware tests all green.
- **Backward compatibility:** `core-auth` promotion parameterized differences via options rather than forks, preserving existing behavior.
- **Documentation:** Decision doc is thorough (309 lines) and will help future maintainers.

## Risks or Blockers

- **None severe.** Track accepted and archived.
- **Medium risk — IM3 wrapper drift:** The IM3 `lib/auth/server.ts` still contains inline `getCookieValueFromHeader` and response builders. Until the IM3 mock harness is updated to stub the new `core-auth` request-guards, this duplication can drift. It is explicitly tracked in `measure/tech-debt.md` with a suggested follow-up track `im3-auth-wrapper-thinning`.

## Recommended Next Actions

1. Schedule `im3-auth-wrapper-thinning` track to update the IM3 auth test mock harness and remove residual inline duplication.
2. Verify in staging that BM2 login/session/role/deactivation revocation behavior is unchanged after the wrapper thinning.
