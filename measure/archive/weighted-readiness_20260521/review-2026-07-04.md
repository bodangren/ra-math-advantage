# Track Review — weighted-readiness_20260521

**Project:** ra-math-advantage  
**Track:** weighted-readiness_20260521  
**Review date:** 2026-07-04  
**Commits reviewed:** 10 commits on 2026-07-03 (all within 24h window)

---

## 1. Summary of Changes

The track was implemented, verified, and archived entirely within the review window. Commits span four TDD phases:

| Phase | Key commit | What changed |
|-------|-----------|--------------|
| Phase 1 — Contract | `6a7845ad` | Added `ReadinessState` type (`ready` \| `nearly_ready` \| `blocked`), optional `readinessScore`/`readinessState` fields on `KnowledgeStateEntry`, Zod schema extensions, and contract tests. |
| Phase 2 — Engine | `a5f12b33` | Implemented `computeWeightedReadiness(B) = Σ(wᵢ·mᵢ) / Σ(wᵢ)`, `createDefaultWeightedReadinessFn`, and switched `getOuterFringe` default from binary gating to weighted readiness (fringe = ready ∪ nearly_ready, blocked excluded). Added 341-line unit test suite. |
| Phase 3 — Projection | `8681c05` | Enriched `computeNodeState` with readiness; added `nearlyReady` bucket to `StudentVisualizationV1` schema/types/visualization; mapped `nearly_ready` in the IM3 Convex `knowledge-state` handler; updated `kst-srs.v2/SPECIFICATION.md` §5.3. |
| Phase 4 — Closeout | `cc1206b1`, `4b8b19d4`, `6fd3e14f`, `94119284` | Updated audit doc, archived track to `measure/archive/`, fixed metadata field naming and plan SHA annotations, updated `measure/tracks.md`. |

Files touched include `packages/knowledge-space-core/src/weighted-readiness.ts` (new), modifications to `mastery-state.ts`, `outer-fringe.ts`, `level-projection.ts`, `index.ts`, `apps/integrated-math-3/convex/student/knowledge-state.ts`, `packages/knowledge-space-practice/src/projections/*`, and the in-repo spec.

---

## 2. Spec Alignment

**Verdict: MET**

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR1 — Weighted readiness formula | Met | `computeWeightedReadiness` implements `Σ(wᵢ·mᵢ) / Σ(wᵢ)`, returns `score=1` when no `prerequisite_for` edges or total weight is zero. |
| FR2 — Three readiness states + configurable thresholds | Met | States `ready`/`nearly_ready`/`blocked` derived against `readyThreshold` (0.80) and `nearThreshold` (0.50); thresholds are overridable per call. |
| FR3 — Outer fringe consumes readiness | Met | `getOuterFringe` now defaults to weighted readiness; blocked nodes excluded; entries carry both `readiness` score and `readinessState`. |
| FR4 — Visualization / IM3 integration | Met | `computeNodeState` enriches non-mastered entries; `StudentVisualizationV1` exposes `nearlyReady`; IM3 handler maps `nearly_ready` into `learnerState`. |
| FR5 — In-repo spec §5.3 | Met | `kst-srs.v2/SPECIFICATION.md` §5.3 documents weight semantics, formula, and default `getOuterFringe` behavior. |
| AC1–AC6 | Met | All acceptance criteria covered by tests; final-acceptance-result.json reports 895 core tests passed, `tsc --noEmit` clean, boundary lint clean, IM3 pipeline tests clean. |

The final acceptance manifest (`measure/archive/weighted-readiness_20260521/final-acceptance-result.json`) lists status `accepted` and anti-patterns A1/A3/A4/A5/A6 as clean.

---

## 3. Code Quality Observations

- **Pure / domain-neutral core.** `weighted-readiness.ts` has no app, Convex, or curriculum imports; it depends only on core types and thresholds.
- **Backward-compatible seam.** `getOuterFringe` retains the optional `readinessFn` parameter; custom functions still include all non-mastered nodes and derive state against thresholds.
- **Type discipline.** `ReadinessState` is kept distinct from `MasteryState` (anti-pattern A6 defense).
- **Test coverage.** 27 new tests across contract, engine, fringe, projection, and visualization modules; edge cases include zero-weight edges, missing prerequisite state entries, non-`prerequisite_for` edges, custom thresholds, and decaying prerequisites.
- **Minor style note.** `packages/knowledge-space-core/src/mastery-state.ts` is missing a trailing newline; this is pre-existing style and does not affect functionality.
- **ReadinessFn threshold limitation.** The custom `readinessFn` path in `getOuterFringe` always uses `MASTERY_THRESHOLDS_DEFAULT` to map the numeric score to a state label; per-call threshold overrides are not propagated into that path. This is consistent with the current `ReadinessFn` signature and not a spec requirement, but worth noting if threshold overrides are ever used with custom readiness functions.

---

## 4. Risks or Blockers

- **Runtime unavailable for re-verification.** The Node runtime is not present in this shell (`node` and `npm` are not on `PATH`), so I could not independently re-run `tsc --noEmit`, lint, or tests. Verification relies on the committed `final-acceptance-result.json` and the clean final-acceptance manifest.
- **Archive completed during window.** The track was moved from `measure/tracks/` to `measure/archive/` in commit `4b8b19d4`; this is expected closeout behavior and does not indicate a blocker.
- **No risks identified in the implementation itself.** All functional requirements, acceptance criteria, and anti-pattern checks are marked complete and clean.

---

## 5. Recommended Next Actions

1. **Confirm CI re-runs pass.** Since the track is archived, ensure the next CI run on `main` still passes with the archived track state.
2. **Propagate threshold override to custom `readinessFn` path** (optional improvement): if callers ever pass both a custom `readinessFn` and threshold overrides, consider threading thresholds into the state-label derivation.
3. **Close the track.** No further code work is required; the review report is now on file at `measure/archive/weighted-readiness_20260521/review-2026-07-04.md`.
