Assembler tests pass (18/18). The 30 semicolon-containing titles are the documented fallback cases (count mismatch between titles and example numbers — the "source gap" the plan's Task 3.4 calls out as ~23 nodes; the higher count is due to double-counting across the aggregate `nodes.json` + `draft-nodes.json`). This is spec-compliant FR-1 fallback behavior, not a regression. I have everything I need.

---

# Code Review: `fix-kst-node-titles_20260615`

## 1. Summary of Changes

Single commit `292deda5` (Jun 16). Root-cause fix plus a large mechanical regeneration:

- **Parser (`parser.ts`)** — `parseWorkedExamplesCell()` rewritten: when the count of `;`-separated titles equals the example-number count, it emits **one `ExtractedWorkedExample` per example number** with its own title; otherwise it falls back to the original single grouped entry. ~27 lines.
- **Tests (`parser.test.ts`)** — assertions updated to the per-example shape (e.g. `1-1` now yields 6 individual entries instead of 2 grouped); same fixture data retained.
- **Regenerated artifacts** — `nodes.json`, `draft-nodes.json`, 9 per-module `nodes.json`, `activity-map.json` (655 activities), 9 `class-period-packages/module-*.json`.
- **Source corrections** — 9 class-period plans, several `aleks/` and lesson source files, `audit/latest.json`, plus track scaffolding and an unrelated `repo-hygiene-remediation` track.

Net: +7,007 / −12,001 lines across 103 files (mostly generated JSON shrinking as grouped titles split out).

## 2. Spec Alignment

| Requirement | Status | Evidence |
|---|---|---|
| FR-1: one entry per example w/ individual title; group fallback on count mismatch | ✅ Met | Logic at `parser.ts:245-261` matches spec verbatim |
| FR-2: regenerate all skill-graph node JSON | ✅ Met | Nodes carry distinct headings, e.g. `"Example 1: Graph Quadratic Functions and State Domain and Range"` |
| FR-3: regenerate `activity-map.json` | ✅ Met | Regenerated (655 activities) |
| FR-4: update fixture-dependent assertions | ✅ Met | `parser.test.ts` 34/34 pass |
| AC: `parseWorkedExamplesCell` split / fallback behavior | ✅ Met | Covered by passing parser tests |
| AC: `npx tsc --noEmit` passes in `math-content` | ⚠️ Partial | Errors exist but are **pre-existing & unrelated** (`problem-families/im1`, `schemas/types.ts` — missing `@types/node`/React namespace); none in changed extraction code |
| AC: `npm run lint` passes | ❌ Environment-blocked | ESLint v9 can't find a flat config (`eslint.config.js`); pre-existing tooling issue, not introduced here |
| AC: parser + assembler tests pass | ✅ Met | Parser 34/34, assembler 18/18 |

Full suite: **290/306 pass; 16 failures all in `problem-families/im1/`** (blueprints/coverage), unrelated to this track and matching the plan's documented baseline.

## 3. Code Quality Observations

- The parser change is minimal, well-commented, and preserves the prior behavior as an explicit fallback — a clean, low-risk fix.
- The guard `titles.length > 0 && titles.length === allNumbers.length` is correct and avoids splitting when titles are absent.
- Tests were strengthened to assert per-title content (not just counts), which genuinely locks in the fix.
- Commit is large but cohesive; the generated-JSON churn is an unavoidable consequence of the regeneration.

## 4. Risks / Blockers

- **30 node titles still contain `;`** (e.g. `"Example 2: Square Roots of Negative Numbers; Products of Pure Imaginary Numbers; ..."`). These are the **intended fallback** (source cells where title count ≠ example count) — the plan's Task 3.4 "source gap" of ~23 nodes (count is inflated by double-counting across aggregate `nodes.json` + `draft-nodes.json`). Not a regression, but they remain less-than-ideal titles tied to upstream source gaps.
- **Mixed-scope commit**: this commit also introduces the unrelated `repo-hygiene-remediation` track and edits `measure/skill-graph-inventory-audit.md`, blurring the track boundary.
- **No automated guard** prevents regression of grouped titles re-creeping into regenerated nodes.

## 5. Recommended Next Actions

1. Treat the 30 fallback titles as a tracked **source-data gap** — add per-example headings to the underlying lesson source files so they split cleanly on next regeneration.
2. Consider a small assembler/extraction test asserting that worked-example node titles for lessons *with* standard headings contain no `;` — a cheap regression guard.
3. Repo-hygiene items (ESLint v9 flat config, `@types/node` in `math-content` tsconfig) are blocking the AC's lint/tsc gate — these belong to the `repo-hygiene-remediation` track and should be resolved there so this track's ACs can be fully green.
4. Split unrelated track scaffolding out of feature commits in future to keep track diffs auditable.

**Verdict:** Core fix is correct, well-tested, and fully meets FR-1–FR-4. Remaining gaps (lint/tsc, residual grouped titles) are pre-existing or upstream source issues, not defects in this change.
