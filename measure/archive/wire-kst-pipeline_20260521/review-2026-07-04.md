# Track Review: wire-kst-pipeline_20260521

**Project:** ra-math-advantage  
**Track:** wire-kst-pipeline_20260521  
**Review date:** 2026-07-04  
**Reviewed commits:** `66e3148f` … `99e1e388` (all within the last 24 hours)  
**Status:** Archived (`measure/archive/wire-kst-pipeline_20260521`)

## Summary of changes

- **Phase 1 — Contract & schema:** Added contract-first types/schemas in `packages/knowledge-space-core` (`mastery-state.ts`, `knowledge-state-engine.ts` signatures, `outer-fringe.ts` signature, `srs-bridge.ts` interface) and reconciled `measure/knowledge-space.md` / `measure/index.md`.
- **Phase 2 — Mastery engine:** Implemented `getKnowledgeState` with time-aware hysteresis (enter at retention ≥ 0.90, exit to decaying at < 0.70, deep-decay fallback), the standalone `getOuterFringe` function with a `readinessFn` seam, `stabilityToRetention`, and a `computeNodeState` wrapper in `level-projection.ts`.
- **Phase 3 — SRS→KST bridge:** Implemented `DefaultSrsToKstBridge` and `buildKstState`; converts SRS card states + objective-proficiency results into evidence, then runs engine → fringe.
- **Phase 4 — Production wiring:** Wired one IM3 production route (`app/student/knowledge-state/page.tsx`) and a Convex internal query (`convex/student/knowledge-state.ts`) that loads the 574-node / 2708-edge IM3 graph, batches SRS reads, and returns a serializable visualization payload.
- **Phase 5 — Docs/audit/closeout:** Updated `measure/knowledge-space-practice-projection-audit.md` from placeholder to wired-IM3 status; added Phase 5 adversarial probes; archived the track and updated `measure/tracks.md`.

## Spec alignment

**Verdict: met**

All six functional requirements (FR1–FR6) and all six acceptance criteria (AC1–AC6) from `spec.md` are addressed. The final acceptance artifact (`final-acceptance-result.json`) records a pass for all five phases, 901 tests across 60 test files, a clean monorepo-boundary lint, and clean root `tsc --noEmit`.

User Manual Verification (UMV) tasks are intentionally marked `[b] deferred:user` per the Measure workflow and are not required by the spec’s acceptance criteria.

## Code quality observations

- Strong contract-first / red-green TDD discipline: every implementation phase is preceded by failing tests and followed by acceptance + adversarial evidence.
- `knowledge-space-core` remains domain-neutral — no app, Convex, curriculum, or SRS-engine imports in the core package; the boundary linter passes.
- Functions are pure and deterministic; `now` is injected rather than calling `Date.now()` inside the engine.
- **Minor concerns:**
  - The production page uses a literal-string `QUERY_REF` (`internal.student.getStudentKnowledgeState`) and silently falls back to an empty visualization on error. This is brittle and can mask runtime integration failures.
  - `getOuterFringe` now defaults to `computeWeightedReadiness` (from the separate weighted-readiness track) rather than simple binary prerequisite gating. This is an enhancement, but it couples the two tracks inside `knowledge-space-core`.
- Non-blocking findings already recorded in final acceptance: 16 pre-existing `knowledge-space-practice` test-file type errors, full IM3 ESLint hangs, and deferred `SPECIFICATION.md` co-location.

## Risks or blockers

- **Low risk:** UMV is deferred; any issues found during manual verification would be handled outside this automated track.
- **Low/medium risk:** The string-based Convex query reference and silent error fallback in the production page could make future integration debugging harder.
- No blockers for closeout; the track is archived and `metadata.json` marks it `done`.

## Recommended next actions

1. Replace the literal `QUERY_REF` with the generated Convex internal reference once the deployment picks it up, and log or surface errors explicitly instead of silently rendering an empty visualization.
2. Re-run the full IM3 ESLint once the pre-existing hang is resolved.
3. Co-locate `kst-srs.v2/SPECIFICATION.md` into `packages/knowledge-space-core/` if the workflow later requires it.
4. Schedule the deferred UMV sessions per `workflow.md`.

---

*Note: Node/npm were not available in this review environment, so test/lint gates were not re-executed live. Alignment assessment is based on the committed diff, plan evidence, and final acceptance artifacts.*
