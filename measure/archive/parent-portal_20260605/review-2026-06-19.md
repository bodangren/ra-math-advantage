# Daily Measure Review — parent-portal_20260605 — 2026-06-19

## 1. Summary of changes

Commits in the last 24 hours completed **Phase 1 — Parent Role, Auth & Linking** and began **Phase 2 — Parent Progress View**.

- Added `parent` role to `packages/core-auth/src/session.ts`, `apps/integrated-math-3/convex/schema.ts` (profiles, auth_credentials), and `apps/integrated-math-3/convex/auth.ts`.
- Added `parent_links` table with `by_parent`, `by_student`, and `by_parent_and_student` indexes.
- Implemented fail-closed guards in `apps/integrated-math-3/lib/auth/parent-server-guards.ts` (`requireParentRequestClaims`, `requireParentServerSessionClaims`).
- Implemented Convex linking mutations/queries in `apps/integrated-math-3/convex/parent/links.ts` (`createParentLink`, `revokeParentLink`, `listParentLinks`).
- Added Red-phase tests: `parent-role-guard.test.ts` (18 tests) and `convex/parent/links.test.ts` (17 tests).
- Added adversarial fix for malformed cookie percent-escapes returning 401 instead of 500.
- Updated `plan.md` and `test-strategy.md` with Phase 1 Green/closeout evidence and Phase 2 Red-phase evidence.

**Uncommitted worktree state:** Phase 2 Red-phase component tests and fixtures (`__tests__/components/parent/*.test.tsx`, `__tests__/_fixtures/parent-portal/*`) are staged but **not committed**. `plan.md` also has an unstaged modification advancing Phase 2 task markers to `[~]`.

## 2. Spec alignment

| Requirement | Status | Notes |
|-------------|--------|-------|
| FR1 — Parent role & auth | **Met** | Role widened; fail-closed guards restrict to linked students. |
| FR2 — Parent↔student linking | **Met** | Create/revoke/list operations implemented; teacher/admin gated; org-match enforced; idempotent. |
| FR3 — Progress view | **Not started** (committed) | Phase 2 UI components not yet committed. |
| FR4 — Multi-student switcher | **Not started** (committed) | Depends on uncommitted Phase 2 components. |
| FR5 — Privacy | **Partial** | Privacy assertions exist in uncommitted Phase 2 Red tests; implementation pending. |
| FR6 — Empty/pending states | **Not started** | Phase 3 scope. |

**Overall Phase 1: met.**  
**Overall Phase 2 (committed portion): partial — Red-phase evidence is documented in `plan.md`, but the actual test files are not in the commit history.**

## 3. Code quality observations

- **Fail-closed by default:** `requireParentRequestClaims` returns 401/403 on every missing-path and wraps `decodeURIComponent` to avoid URIError surfacing as 500.
- **Type safety:** Convex wrappers use `v.id(...)` validators and typed `MutationCtx`/`QueryCtx`.
- **Boundary clean:** No app/Convex imports in shared `session.ts`; only the IM3 app is touched.
- **State mismatch risk:** The committed `plan.md` describes Phase 2 Red-phase evidence and lists uncommitted files as "folded into Red-phase commit," but those files are not actually committed. This creates a discrepancy between the plan's claims and git HEAD.

## 4. Risks or blockers

- **Moderate — uncommitted Phase 2 artifacts:** Staged `ParentDashboard.test.tsx`, `StudentSwitcher.test.tsx`, `parent-privacy.test.tsx`, and fixtures are not committed. If the worktree is cleaned or another automation session runs, this evidence could be lost.
- **Low — redirect path encoding:** `requireParentServerSessionClaims` does not URL-encode `loginRedirectPath` in the redirect query string; paths containing `?` or `&` would be malformed.
- **Low — no parent projection consumption yet:** `packages/knowledge-space-practice/src/projections/visualization.ts::projectParentVisualization` has zero callers; Phase 2 will be the first consumer.

## 5. Recommended next actions

1. Commit the staged Phase 2 Red-phase test files and fixtures, or explicitly discard them if they are not ready.
2. Implement `ParentDashboard.tsx` and `StudentSwitcher.tsx` to satisfy the Phase 2 Red tests.
3. Add URL-encoding for `loginRedirectPath` in `requireParentServerSessionClaims`.
4. Run the Phase 2 closeout gate: `npx vitest run __tests__/components/parent/` and boundary lint.
