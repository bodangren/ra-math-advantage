# Daily Review — Next-Skill Planner Production Wiring

**Project:** ra-math-advantage  
**Track:** `next_skill_planner_prod_wiring_20260621`  
**Date:** 2026-06-22  
**Commits reviewed (last 24h):**
- `c6a9302d` — measure(plan): correct Phase 3 boundary note
- `9459ffc0` — test(planner-prod-wiring): add Phase 3 Red tests for student dashboard planner wiring
- `123e8f18` — docs(planner-prod-wiring): mark Phase 2 Green tasks [x]
- `3aafe88b` — feat(planner-prod-wiring): Phase 2 Green — expose getStudentVisualization internalQuery
- `f05ff9db` — docs(planner-prod-wiring): verify Phase 2 Red phase state
- `417b8d3e` — docs(planner-prod-wiring): add Phase 2 track docs and Red verification
- `3cbb99f1` — docs(tracks): mark next-skill planner production wiring as in progress
- `a574f407` — test(planner-prod-wiring): add Phase 2 Red tests for backend exposure
- `73228657` — test(planner-prod-wiring): add Phase 1 Red tests with tightened student-surface contract
- `cd86d6e9` — test(planner-prod-wiring): Phase 1 Red — caller-scan + getStudentVisualization API seam

## Summary of changes

- **Phase 1 Red:** committed `apps/integrated-math-3/__tests__/planner-prod-wiring.test.ts` asserting (a) the planner output has a production backend caller, (b) `internal.student.getStudentVisualization` is registered, and (c) a student-facing surface consumes the planner recommendations.
- **Phase 2 Red + Green:** committed `studentVisualization.test.ts` and a frozen fixture, then implemented `getStudentVisualization` as an `internalQuery` in `apps/integrated-math-3/convex/student.ts`, exporting a mock-ctx-testable `getStudentVisualizationHandler` and enabling `resolveJsonModule` in `convex/tsconfig.json`.
- **Phase 3 Red:** committed `apps/integrated-math-3/__tests__/student/dashboard-planner.test.tsx` for the dashboard recommendation panel and empty-state; the dashboard page was reverted to HEAD so the Green role owns the UI wiring commit.
- **Measure hygiene:** `metadata.json`, `spec.md`, `test-strategy.md`, and `plan.md` were scaffolded/updated; `measure/tracks.md` marks the track in progress.

## Spec alignment

- **FR-1 (backend query)** — **met**. `getStudentVisualization` / `getStudentVisualizationHandler` now expose the planner output through a real Convex internal query.
- **FR-2 (student-facing consumer)** — **partial**. Phase 3 Red tests are committed, but the dashboard page implementation is still dirty/uncommitted in the working tree (the plan explicitly reverted it to HEAD for the Green role). The new untracked `apps/integrated-math-3/components/student/RecommendedNextPanel.tsx` also suggests active Green work.
- **FR-3 (planner math preserved)** — **met**. No changes to `packages/knowledge-space-practice/src/projections/visualization.ts` or planner math; existing projection tests remain green.
- **FR-4 (runtime tests proving caller chain)** — **partial**. Tests exist for Phases 1–2 and Phase 3 Red is committed, but the full end-to-end caller chain will only be proven once Phase 3 Green lands.
- **FR-5 (non-test production caller proof)** — **partial**. The P1 source-scan test now sees the backend query; it still needs the committed student page import to satisfy the tightened “student-facing production consumer” assertion.

## Code quality observations

- Strong TDD discipline: Red tests were committed before their Green implementations, and the plan records stash-and-rerun proof that Red tests fail without the implementation.
- Mock-ctx testability is handled correctly by exporting the named handler function separate from the `internalQuery` wrapper.
- The frozen 4-node/2-edge fixture in `__tests__/convex/_fixtures/student-viz-fixture.ts` keeps tests deterministic.
- Boundary discipline is mostly good: `graph.db` was not committed in Red phases and the dashboard page was intentionally left out of the Red commit.
- The working tree contains the Phase 3 dashboard wiring, but it is not yet committed, leaving the track in a mid-phase state.

## Risks or blockers

- **Phase 3 Green is uncommitted.** Until `app/student/dashboard/page.tsx` and `RecommendedNextPanel.tsx` are committed, the track has no delivered student-facing consumer.
- **Dirty-worktree noise.** The repo has many unrelated modified files; this previously triggered supervisor `gate_mid` false positives on other tracks and could do so again here.
- **Graph.db freshness.** The plan notes graph.db mutations are staged locally but not committed; the closeout phase needs a fresh, committed graph.
- **Acceptance criterion AC-1** (student-facing production surface displays planner recommendations) is still open.

## Recommended next actions

1. Commit the Phase 3 dashboard page wiring and `RecommendedNextPanel.tsx` as a single Green commit.
2. Run targeted tests: `planner-prod-wiring`, `studentVisualization`, `dashboard-planner`.
3. Verify the production-caller source-scan test passes with the committed student page import.
4. Refresh and commit `graph.db` with a clear `chore(graph)` message.
5. Update `plan.md` Phase 3 tasks to `[x]` and run Phase 4 closeout only after AC-1 is proven.
