# Code Review: `spec-compliance-and-process-integrity_20260612`

Review date: 2026-06-29  
Base: changes in the 24 hours preceding 2026-06-29T21:04Z  
HEAD: `87737db5` on `master` (24 commits ahead of origin)

## Summary of changes

35 commits touched 212 files (+3,599 / −406). Work on this track in the last day covers:

- **Phase 3 (FR-5 typed JSDoc):** wired the typed-param/returns guard to CI (`.github/workflows/jsdoc-typed-params.yml`), an opt-in pre-commit hook (`scripts/git-hooks/pre-commit-fr5-typed-params`), and three npm scripts (`guard:fr5-typed-params*`).
- **Phase 4 (exported Convex surface + `@throws`/`@returns`):** added JSDoc to 131 newly-documented exported wrappers across `apps/integrated-math-3/convex/` (197/197 now covered), completed an `@throws` audit over 81 production throw sites, and added `@throws` coverage in 8 `lib/` files. Added `check-jsdoc-exported-convex-im3.sh` with a bad-sample fixture and A4 vacuous-pass defense.
- **Phase 5 (verification process integrity):** reset all 9 archived `jsdoc-comments_20260526` verification reports to `pending` with placeholder verifier/timestamp, and hardened `check-phase-verification-guards.sh` to reject automation/measure-mid/bot/ai-agent/llm/machine verifiers and pending/rejected results.
- **Support / archive:** refreshed track `_artifacts`, archived the `math-content-test-suite-repair_20260626` track, and closed out the `code-review-remediation_20260624` track.

Current worktree is clean; one stash remains (`stash@{0}`) containing the earlier uncommitted Phase 3 Green file set plus unrelated `measure/tracks.md` and parent-portal archival deletes.

## Spec alignment

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR-1 Clean repository state | **Met** | `master` checked out, worktree clean, detached HEAD resolved earlier, stashes documented/intentional. |
| FR-2 Revert FR-6 declaration-style conversions | **Met** | Plan records reverts of `DropdownMenuShortcut`, `PlaceholderComponent`, and `staticTimestamp`; available FR-6 guard reports 0 violations for BM2 components scope. |
| FR-3 Type annotations on `@param`/`@returns` | **Met** | `check-jsdoc-typed-params.sh`: 593/593 typed in `apps/integrated-math-3/convex/`; 1811/1811 typed across full Phase 3–8 scope per plan. |
| FR-4 Missing `@throws` / `@returns` | **Met** | Throws audit covers 81 production throw sites with 0 uncovered after review-A fix; `saveCardsHandler` `@returns` verified. |
| FR-5 JSDoc on exported Convex wrappers | **Met** | `check-jsdoc-exported-convex-im3.sh`: 197/197 exported wrappers documented. |
| FR-6 Reset reports + real human verification | **Partial** | All 9 reports reset to `pending`; real human verification is **not yet performed** (deferred:user task). |
| FR-7 Harden verification guards | **Met** | Hardened guard rejects automation self-approval and pending/rejected states; fixture self-test demonstrates correct classification. |
| FR-8 kst-lesser-holes Phase 1 quality | **Missed** | Phase 6 tasks remain unchecked; no commits in the last 24 hours address schema tightening, rule deduplication, or test expansion. |

## Code quality observations

- **Guard quality is good:** both new guards include bad-sample fixtures that fail by design, preventing vacuous-pass. The FR-5 CI workflow is intentionally separate from `ci.yml` because `ci.yml` ignores `measure/**` changes.
- **Pre-commit hook follows repo convention:** opt-in `scripts/git-hooks/pre-commit-fr5-typed-params` mirrors the existing graph.db hook pattern and is skippable via `SKIP_FR5_TYPED_PARAMS=1`.
- **Artifact discipline:** each phase closeout records `_artifacts/*.log` and JSON files, making review auditable.
- **Plan.md noise:** the plan now contains lengthy restart classifications (restarts #2–#7) that document a now-resolved supervisor-gate false positive. Consider archiving or condensing these sections once Phase 3 Green is committed.
- **Stash hygiene:** `stash@{0}` still holds Phase 3 Green changes that were un-stashed from the worktree. The plan documents the stash as owned by a parallel process, but an open stash is a latent risk if the process does not return.

## Risks or blockers

1. **Human-in-the-loop blocker (Phase 5).** All verification reports are intentionally `pending`. The track cannot accept Phases 1–9 of `jsdoc-comments_20260526` until a human verifier runs `measure/workflow.md` Steps 1–10 and updates the reports with a real name/timestamp.
2. **Uncommitted stash (Phase 3).** The 27-file Phase 3 Green work remains in `stash@{0}`. If the parallel daily-automation process does not pop and commit it, the FR-5 guard could later appear to fail when the stash is lost.
3. **Unstarted Phase 6.** FR-8 (kst-lesser-holes schema/test remediation) has no progress and is not blocked on external verification.
4. **Unpushed commits.** `master` is 24 commits ahead of origin. Ensure these are pushed (or intentionally held back) so CI can run the new workflows.
5. **No unified FR-6 guard found.** The available FR-6 script only covers `apps/bus-math-v2/components/`; other scopes listed in the plan rely on documented per-file assertions rather than a single runnable guard.

## Recommended next actions

1. **Complete Phase 5 human verification:** a human reviewer should inspect the archived phase reports, run the real lint/typecheck/test commands, and update each report with genuine `VERIFIED_BY` / `VERIFIED_AT` values.
2. **Resolve `stash@{0}`:** either pop and commit the Phase 3 Green work as a single `feat(spec-compliance)` commit, or drop it intentionally and re-run the codemod if needed.
3. **Start Phase 6 (FR-8):** tighten `level-projection.ts` and `progress-trend.ts` schemas, deduplicate `EDGE_ENDPOINT_RULES`, expand `transfers_to` and `public-api-contract` negative tests, and remove stale Red-phase comments.
4. **Push `master` to origin** so the new `jsdoc-typed-params.yml` workflow is available in CI.
5. **Run Phase 7 final verification** once Phases 5 and 6 are closed: refresh `graph.db`, run all guards, record real command output, and commit the checkpoint.
