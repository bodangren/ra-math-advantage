# Daily Measure Review — repo-hygiene-remediation_20260616 — 2026-06-19

## 1. Summary of changes

A single commit addressed Task 3.1 of the repo-hygiene plan:

- Updated `apps/bus-math-v2/__tests__/components/user-menu.test.tsx` to mock the package-relative `AuthProvider.tsx` path inside `@math-platform/app-shell`.
- Added a defense-in-depth mock for `@math-platform/app-shell/auth`.
- Updated `plan.md` to mark Task 3.1 done and note the remaining 4 failing tests (Task 3.1b).

The fix changed the test from 9 failed (all `useAuth must be used within an AuthProvider`) to 5 passed / 4 failed. The remaining 4 failures assert a role-aware Dashboard link that `UserMenu.tsx` does not yet render.

## 2. Spec alignment

| Requirement | Status | Notes |
|-------------|--------|-------|
| FR-5 — Fix BM2 pre-existing test reds | **Partial** | Mock-resolution root cause fixed, but the test still fails because the component under test does not implement the asserted Dashboard link behavior. |
| FR-1 / FR-2 / FR-3 / FR-4 / FR-6 / FR-7 / FR-8 | **No change** | No commits for these requirements in the period. |

**Overall: partial.** The committed change fixes the *mocking infrastructure* but not the *feature behavior* the tests describe.

## 3. Code quality observations

- The mock-path computation inside `vi.hoisted` correctly handles the monorepo layout and package-relative import inside `app-shell`.
- The test now exercises real component rendering rather than failing at import time.
- The remaining failures are honest failures: they reveal a gap between test expectations and component implementation.

## 4. Risks or blockers

- **Moderate — spec/test drift:** 4 tests assert a Dashboard link with role-aware hrefs (`/student/dashboard`, `/teacher/dashboard`) that `UserMenu.tsx` does not implement. This means the test suite is red and the acceptance criterion (AC-8: BM2 UserMenu tests pass) is not met.
- **Low — Task 3.1b deferred:** The plan correctly defers the Dashboard link implementation, but doing so leaves the track in a non-green state.

## 5. Recommended next actions

1. Implement the role-aware Dashboard link in `packages/app-shell/src/components/UserMenu.tsx` (or update the tests if the behavior is no longer required).
2. After Dashboard link lands, re-run `npx vitest run apps/bus-math-v2/__tests__/components/user-menu.test.tsx` to reach 9/9 passing.
3. Continue with Task 3.2 (GradebookDrillDown flaky timeout) and Phase 4 React 19 lint violations.
