Write was denied — I'll present the report inline instead.

---

# Review — repo-hygiene-remediation_20260616 (2026-06-17)

## 1. Summary of changes (last 24h)

| Commit | Scope |
|--------|-------|
| `292deda5` | Parser fix (`split semicolon-separated titles 1:1`) + full curriculum regeneration. 103 files, **+7,007 / −12,001**. Also bundles the `fix-kst-node-titles` and this track's spec/plan/index files. |
| `8cff1b2e` | Plan bookkeeping — mark Phase 1 complete. |
| `93ccf0b9`, `9fe7b72a`, `8e7d4767` | Daily-review report artifacts (measure tooling, outside track scope). |

**Uncommitted working tree (not clean):**
- `apps/bus-math-v2/__tests__/components/user-menu.test.tsx` — in-progress Task 3.1 fix
- `measure/tracks/.../plan.md` — Task 3.1 marked `[~]`
- `review-2026-06-17.md` — empty/untracked

## 2. Spec alignment

| Req | Status | Notes |
|-----|--------|-------|
| FR-1 Clean working tree | **Missed** | Tree still dirty (3 paths). **AC-1 fails.** |
| FR-2 Resolve 6 stashes | **Met** | `git stash list` empty (AC-2). No commit trail, so preserve-vs-drop choices are unverifiable from history. |
| FR-3 Phase 3 JSDoc | Not started | — |
| FR-4 Phase 7 verify | **Missed** | No verification recorded. |
| FR-5 BM2 test reds | **Partial (broken)** | UserMenu attempt does not pass; GradebookDrillDown untouched. **AC-8 fails.** |
| FR-6 IM3 React 19 lint | **Missed** | Phase 4 not started. **AC-7 fails.** |
| FR-7 tsc | Not run | AC-5/6 unverified. |
| FR-8 lint zero errors | Not run | AC-3/4 unverified. |

**Realized:** Phase 1 ✓, Phase 2 ✓, Phase 3 in progress (failing), Phases 4–5 not started — roughly **2 of 5 phases**.

## 3. Code quality observations

- **Task 3.1 fix is incorrect — verified failing.** The test was re-pointed to mock `@math-platform/app-shell/auth`, but `UserMenu.tsx:3` imports `useAuth` via the **relative path** `../auth/AuthProvider`, which Vitest resolves to a different module record than the `./auth` subpath export. The mock never intercepts the real hook, so all 9 tests still throw `useAuth must be used within an AuthProvider` (I ran it: **9 failed / 11 passed**). Fix: mock the module UserMenu actually imports (the resolved `packages/app-shell/src/auth/AuthProvider`), or wrap in a real/stub `<AuthProvider>` as the plan originally specified.
- The plan's Task 3.1 sub-items ("wrap all 9 cases in AuthProvider") were abandoned for a mock-swap that doesn't work; the sub-checkboxes correctly remain unchecked.
- **Commit `292deda5` is oversized and mixed-purpose** — a real parser behavior change + a 13k-line `activity-map.json` regeneration + two tracks' planning files in one commit. This violates **NFR-3** (atomic, one logical fix per commit) and makes the curriculum diff hard to review or revert independently.

## 4. Risks / blockers

- **Working tree left dirty mid-task** — the track's central goal (FR-1/AC-1) is currently regressed by its own in-flight work.
- **Broken test must not be committed as-is** — would break NFR-1 (per-commit gate).
- **Stash resolution is irreversible and untraceable** — AC-2 is green, but with no branch/commit evidence there's no proof the 5 "preserve" stashes were branched rather than dropped. Potential silent loss of `learning-efficacy-analytics` WIP.
- Phases 4–5 (lint, tsc, React 19 rules — the bulk of the acceptance criteria) untouched; track ~40% complete.

## 5. Recommended next actions

1. **Fix Task 3.1 correctly** — point `vi.mock` at the path UserMenu imports (`../auth/AuthProvider` as resolved within `@math-platform/app-shell`), confirm 20/20 pass, commit atomically.
2. **Do Task 3.2** (GradebookDrillDown timeout) and run the full BM2 suite for AC-8.
3. **Restore a clean tree** at each phase boundary so AC-1 actually holds.
4. **Verify stash preservation** — `git branch --list '*efficacy*' '*analytics*'` to confirm WIP was branched, not dropped; record disposition in the plan.
5. Proceed to **Phase 4** (IM3 React 19 violations) and **Phase 5** verification (`tsc --noEmit`, lint, tests); record results to close AC-3 through AC-7.
6. Keep curriculum regeneration in its own commit, separate from planning edits (NFR-3).

---

Note: I attempted to write this to `review-2026-06-17.md` (the empty placeholder already in the track), but the Write was denied by the current permission mode. Let me know if you'd like it saved there and I'll retry.
